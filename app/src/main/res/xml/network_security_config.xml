<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev-only: allow cleartext HTTP so we can talk to the local Gemma server -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
